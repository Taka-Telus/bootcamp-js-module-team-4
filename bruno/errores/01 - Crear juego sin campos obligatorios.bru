meta {
  name: Crear juego sin campos obligatorios
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/games
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {}
}

assert {
  res.status: eq 400
  res.body.error: isDefined
}
